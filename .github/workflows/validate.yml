name: Validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    uses: Xeltor/XelsDalamud.Workflows/.github/workflows/plugin-validate.yml@main
    with:
      plugin_project_path: XelsTweaks/XelsTweaks.csproj
      dotnet_version: 10.0.x
      internal_name: XelsTweaks

